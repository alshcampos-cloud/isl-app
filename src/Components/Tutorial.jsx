// Tutorial temporarily disabled - will be redesigned in future update
// Users can learn by exploring the app naturally

export default function Tutorial({ user, isActive, onClose }) {
  // Tutorial disabled - return null
  // Will be redesigned with better UX in future update
  return null;
}
